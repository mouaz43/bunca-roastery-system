<section class="card">
  <h1 class="h1">Anmelden</h1>
  <p class="p">Bitte melde dich an, um Bestellungen zu erstellen und den Status zu verfolgen.</p>

  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <form method="POST" action="/login" class="form">
    <label class="label">
      E-Mail
      <input class="input" type="email" name="email" placeholder="name@bunca.de" required />
    </label>

    <label class="label">
      Passwort
      <input class="input" type="password" name="password" placeholder="••••••••" required />
    </label>

    <button class="btn btn-primary" type="submit">Einloggen</button>
  </form>

  <div class="hint">
    <span class="muted">Hinweis:</span> Falls du keinen Zugang hast, bitte die Rösterei kontaktieren.
  </div>
</section>
